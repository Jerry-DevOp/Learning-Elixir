import Logger
defmodule Num do
  # alias Identicon
  def take do
 "hello"
end
end

defmodule Jerry do

def sum do
  alias Math.List, as: List
var = [1, 2, 3, 4]
np = Enum.sum(var)
# info("Hello Jerry")

info("sum is #{np}")
# Num.take()

Identicon.hash_input("ghggh")
end
end
